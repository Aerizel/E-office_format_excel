import { groupModel } from "../../models/formatExcel/GroupModel";
import { orgModel } from "../../models/formatExcel/OrganizeStructureModel";

export default function formatPermission(orgData: orgModel[], groupData: groupModel[]) {

}